package org.rangiffler.utils;

import org.junit.jupiter.params.provider.Arguments;

import java.util.stream.Stream;

public class Params {

    static Stream<Arguments> negativeLoginPassword() {
        return Stream.of(

        );
    }
}
